name: vigov_mobile
description: "ViGov — Ứng dụng công dân (Android + iOS)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^16.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  intl: ^0.20.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
